import React, { Fragment, Component } from 'react';

class Dashboard extends Component {
    render() {
        return (
            <Fragment>
                Dashboard
            </Fragment>
        );
    }
}

export default Dashboard;